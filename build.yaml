targets:
  $default:
    builders:
      freezed:
        generate_for:
          include:
            - lib/**/*state.dart
            - lib/models/**/*.dart
      json_serializable:
        generate_for:
          include:
            - lib/models/**/*.dart
        options:
          any_map: true
          explicit_to_json: true
